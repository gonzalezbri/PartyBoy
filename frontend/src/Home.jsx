import { Container, Typography } from '@mui/material';

const Home = () => {
  return (
    <Container maxWidth="md">
      <Typography variant="h1">Welcome to PartyBoy!</Typography>
      <Typography variant="body1">
        Let's Party
      </Typography>
    </Container>
  );
};

export default Home;