import { AppBar, Toolbar, Typography } from '@mui/material';

const Appbar = () => {
  return (
    <AppBar>
      <Toolbar>
        <Typography variant="h6">PartyBoy</Typography>
      </Toolbar>
    </AppBar>
  );
};

export default Appbar;

